export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh" # This loads nvm
alias crunchy='cd $HOME && mkdir -p crunchy && cd $_ && touch peanutbutter.txt && echo "Is the best!" >> peanutbutter.txt && echo $PWD >> peanutbutter.txt && chmod 666 peanutbutter.txt'
